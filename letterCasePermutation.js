var letterCasePermutation = function(S) {
  // init a result array
  // init a helper function, with string as input
    // if string is one w


  // run helper function with current string
  // return result array
};